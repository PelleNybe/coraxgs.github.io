Fix DOM-based XSS vulnerability in app.js

- Escaped all user inputs used in DOM insertions (`innerHTML`) by running `escapeHTML` helper method.
- Addressed XSS vectors specifically in component templates: `ProjectCardElement.render()`, `log()`, `renderEvents()` and `renderPosts()`.
- Addressed the vulnerability issue recorded in the `.jules/sentinel.md` journal.
